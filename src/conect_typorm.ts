import "reflect-metadata"


import { createConnection, Connection } from "typeorm";

createConnection({
    type: "postgres",
    host: "localhost",
    port: 5432,
    username: "e",
    password: "e",
    database: "e"
});
